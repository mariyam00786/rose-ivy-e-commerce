# Flower Shop MERN

## Setup
1. cd server && cp .env.example .env
2. cd client && cp .env.example .env
3. cd .. && npm install
4. npm run dev

## Seed
npm --prefix server run seed

## Notes
- Backend runs on port 5000
- Frontend runs on Vite port 5173
