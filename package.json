{
  "name": "flower-shop-mern",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix client && npm install --prefix server",
    "build": "npm run install-all && npm run build --prefix client && rm -rf public && mv client/dist public",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "node server/server.js",
    "seed": "node server/seed.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "bcryptjs": "^2.4.1",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.0",
    "dotenv": "^16.4.0",
    "express": "^4.19.0",
    "express-rate-limit": "^8.5.2",
    "helmet": "^8.2.0",
    "jsonwebtoken": "^9.0.1",
    "mongoose": "^8.0.3",
    "morgan": "^1.11.0",
    "multer": "^2.1.1",
    "nodemailer": "^8.0.10",
    "razorpay": "^2.9.6",
    "stripe": "^16.0.0"
  }
}
